# Simulation Uncertainty

Replications use deterministic seeds derived from the master seed, simulation case, experiment, replication and stream
name. Separate streams are used for lateness, cancellations, no-shows and service durations.

Confidence intervals use a deterministic normal approximation over replication metrics. Intervals communicate
synthetic simulation variability only and are not real-world clinical guarantees.
