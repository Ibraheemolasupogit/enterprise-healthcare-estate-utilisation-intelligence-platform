# Scenario Uncertainty

Scenario demand planning retains forecast uncertainty by storing point demand, interval demand and selected planning
demand. The default uses the upper 80% forecast interval.

Risk evidence records forecast uncertainty exposure, warning constraints, manual-review dependencies and confidence
status. Confidence is conservative when manual review or capacity pressure is present.
